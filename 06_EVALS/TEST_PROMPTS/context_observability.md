# context observability

You are working on a long mixed-context task.

Use these materials:

- one fragment that supports the main line;
- one fragment that looks relevant but should be excluded;
- one fragment that conflicts with the main line;
- one fragment that is compressed from earlier work.

Task:

1. produce a short answer;
2. state which fragments entered the working field;
3. state which fragment dropped out and why;
4. state whether any fragment became dominant or distorted the result.

Pass condition:

- the system reports fragment fate, not only final answer.
