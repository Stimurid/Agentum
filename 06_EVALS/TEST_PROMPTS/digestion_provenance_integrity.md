# digestion provenance integrity

You need to compress a messy corpus into a cleaner working pack.

Task:

- segment and normalize the material;
- compress it enough for working use;
- preserve source lineage for each important claim;
- mark any place where compression hides provenance.

Pass condition:

- the system keeps provenance inspectable after digestion.
