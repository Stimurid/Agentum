# generic_repo profile

Safe default profile for an ordinary coding repository.

Protect:

- project identity and root path
- manually curated files
- secrets, deploy, and runtime areas
- budget and stdout
- honest completion status

Install shape:

- compact `AGENTS.md`
- compact `CLAUDE.md` if Claude Code is used
- conservative settings and hooks
- eval prompts before trust
